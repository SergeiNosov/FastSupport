

@model Dictionary<string, int>

@{
    ViewData["Title"] = "Обращение";

    if(Model.Count == 0)
    {
        Model["Здравствуйте, напишите нам своё обращение сюда или на почту support@gmail.com"] = 0;
    }
}


<html>
<head>
    <link rel="stylesheet" href="~/CSS/messages.css" type="text/css" />
</head>

<body>
    <div class="container d-flex justify-content-center">
        <div class="card mt-5">


            @foreach (var message in Model)
            {/*
                @if (message.Value == 1)
                {
                    <div class="d-flex flex-row p-2">

                    <div class="chat ml-2 p-3">@Html.Raw(message.Key)</div>
                    <img src="https://img.icons8.com/color/48/000000/circled-user-male-skin-type-7.png" width="40" height="40">
                    </div>
                }
                else
                {

                    <div class="d-flex flex-row p-2">
                        <img src="https://img.icons8.com/color/48/000000/circled-user-female-skin-type-7.png" width="40" height="40">
                        <div class="chat ml-2 p-3">@Html.Raw(message.Key)</div>
                    </div>

                }*/

            <div class="d-flex flex-row p-2">
                @if (message.Value == 0)
                {
                    <img src="https://img.icons8.com/color/48/000000/circled-user-female-skin-type-7.png" width="40" height="40">
                }
                else
                {
                    <img src="https://img.icons8.com/color/48/000000/circled-user-male-skin-type-7.png" width="40" height="40">
                }
                <div class="chat ml-2 p-3">@Html.Raw(message.Key)</div>
            </div>
            }


           


            <form action="/Messages/SendMessage" method="get">

                <div class="form-group px-3"> <textarea type="text" name="mail" class="form-control" rows="3" placeholder="Ваша почта (ivan@example.com)"></textarea> </div>
                <div class="form-group px-3"> <textarea type="text" name="message" class="form-control" rows="3" placeholder="Введите сюда своё обращение"></textarea> </div>




                <button type="submit" class="btn btn-success">Отправить</button>


            </form>
        </div>


    </div>

</body>

</html>